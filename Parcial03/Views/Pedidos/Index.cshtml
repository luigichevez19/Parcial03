@model IEnumerable<Parcial03.Models.Pedidos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.clientes.nombres)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.productos.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fechaPedido)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fechaEntrega)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.clientes.nombres)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.productos.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fechaPedido.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fechaEntrega.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
